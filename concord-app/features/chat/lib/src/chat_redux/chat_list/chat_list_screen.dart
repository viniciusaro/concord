import 'package:chat/src/chat_redux/chat_list/chat_list_screen_view_model.dart';
import 'package:chat_team_library/chat_team_library.dart';

class ChatListScreen extends StatelessWidget {
  final ChatListScreenViewModel viewModel;

  const ChatListScreen({Key? key, required this.viewModel}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
